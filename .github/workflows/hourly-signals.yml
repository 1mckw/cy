name: Hourly Crypto Alerts (Bybit TOP50)

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/hourly-signals.yml"
      - "scan_signals.py"
      - "universe.py"
      - "static/**"
      - "ardr.py"
      - "trendlines.py"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: hourly-crypto-alerts
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        if: github.event_name != 'push'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Scan Bybit TOP50 contracts
        if: github.event_name != 'push'
        env:
          PYTHONUNBUFFERED: "1"
          PYTHONIOENCODING: utf-8
        run: python -u scan_signals.py

      - name: Commit signal report
        if: github.event_name != 'push'
        run: |
          git add signals/latest.json signals/latest.html signals/index.html signals/chart-packs.json
          if git diff --staged --quiet; then
            echo "No signal file changes"
            exit 0
          fi
          git commit -m "chore: update crypto hourly touch alerts (Bybit TOP50)"
          git pull --rebase origin main || true
          git push origin main
        env:
          GIT_AUTHOR_NAME: github-actions[bot]
          GIT_AUTHOR_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
          GIT_COMMITTER_NAME: github-actions[bot]
          GIT_COMMITTER_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com

      - name: Prepare Pages site
        run: |
          mkdir -p _site
          cp signals/latest.html _site/index.html
          cp signals/latest.json _site/latest.json
          cp signals/chart-packs.json _site/chart-packs.json
          touch _site/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: scan
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
